const calendarService = {
  getEvents: async () => {
    return [];
  },

  createEvent: async (data) => {
    return data;
  },

  deleteEvent: async (id) => {
    return id;
  },
};

export default calendarService;