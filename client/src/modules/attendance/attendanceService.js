const attendanceService = {
  getAll: async () => {
    return [];
  },

  create: async (data) => {
    return data;
  },

  update: async (id, data) => {
    return data;
  },

  delete: async (id) => {
    return id;
  },
};

export default attendanceService;