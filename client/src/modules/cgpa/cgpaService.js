const cgpaService = {
  getSemesters: async () => {
    return [];
  },

  addSemester: async (data) => {
    return data;
  },

  deleteSemester: async (id) => {
    return id;
  },
};

export default cgpaService;