const examService = {
  getExams: async () => {
    return [];
  },

  createExam: async (data) => {
    return data;
  },

  deleteExam: async (id) => {
    return id;
  },
};

export default examService;